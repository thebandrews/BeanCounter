<phone:PhoneApplicationPage
    x:Class="BeanCounter.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="bean counter" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0" Foreground="#FF745F48"/>
            <TextBlock Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}" Foreground="#FF81D878">
            	<Run Text="food "/>
            	<Run Text="drink"/>
            </TextBlock>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Grid HorizontalAlignment="Left" Height="587" Margin="10,10,0,0" VerticalAlignment="Top" Width="436">
                <TextBlock HorizontalAlignment="Left" Height="77" TextWrapping="Wrap" Text="remaining beans = X" VerticalAlignment="Top" Width="436"/>
                <Button x:Name="bean_1" Content="bean+=1" HorizontalAlignment="Left" Height="100" Margin="10,57,0,0" VerticalAlignment="Top" Width="416" FontFamily="Segoe WP SemiLight" Click="bean_1_Click"/>
                <Button x:Name="bean_3" Content="bean+=3" HorizontalAlignment="Left" Height="100" Margin="10,162,0,0" VerticalAlignment="Top" Width="416" FontFamily="Segoe WP SemiLight" Click="bean_3_Click"/>
                <Button x:Name="bean_5" Content="bean+=5" HorizontalAlignment="Left" Height="100" Margin="10,267,0,0" VerticalAlignment="Top" Width="416" FontFamily="Segoe WP SemiLight" Click="bean_5_Click"/>
                <Button x:Name="bean_10" Content="bean+=10" HorizontalAlignment="Left" Height="100" Margin="10,372,0,0" VerticalAlignment="Top" Width="416" FontFamily="Segoe WP SemiLight" Click="bean_10_Click"/>
                <Button x:Name="bean_custom" Content="bean+=custom" HorizontalAlignment="Left" Height="100" Margin="10,477,0,0" VerticalAlignment="Top" Width="416" FontFamily="Segoe WP SemiLight"  Click="bean_custom_Click"/>
            </Grid>

        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton 
                IconUri="/Assets/Images/Dark/feature.settings.png" 
                Text="setup" 
                x:Name="newTaskAppBarButton" 
                Click="newTaskAppBarButton_Click"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>



</phone:PhoneApplicationPage>